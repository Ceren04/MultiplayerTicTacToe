class GameProtocol:
    # Message types ve serialization
    @staticmethod
    def serialize_move(row, col, player)
    @staticmethod
    def serialize_game_state(board, current_player)
    @staticmethod
    def deserialize_message(data)